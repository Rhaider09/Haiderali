{
  "build": {
    "env": {
      "COMPOSER_ALLOW_SUPERUSER": "1"
    },
    "buildCommand": "composer install --no-interaction --prefer-dist --optimize-autoloader && php artisan key:generate && php artisan migrate --force"
  },
  "start": {
    "command": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
